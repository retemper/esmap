# @esmap/vite-plugin

::: warning WIP
This page is under construction.
:::

Vite plugin for esmap. Handles manifest generation for import map deployments and configures ESM externals so shared dependencies are loaded from the import map at runtime.

## Installation

```bash
pnpm add -D @esmap/vite-plugin
```
